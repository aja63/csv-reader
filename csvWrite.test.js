const fs = require("fs");
const parse = require("csv-parse");
const cityList = require('./createList');
const write = require('./csvWrite');


let csvFile = "countries.csv";

test('Can write csv', () => {

});